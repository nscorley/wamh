import React from 'react';

const MainNavigation = () => (
  <div className="main-nav">
    This will be the main navigation, which runs throughout all of the pages.
  </div>
);

export default MainNavigation;
